From 51d8ea97b51f2754c1cda7ecd16b4230ecf4b508 Mon Sep 17 00:00:00 2001
From: Nathan Chancellor <nathan@kernel.org>
Date: Mon, 30 Mar 2026 09:58:11 +0200
Subject: [PATCH 13/20] WIP/REPORTED: net: enetc: Avoid clang
 -Wsometimes-uninitialized in enetc_pf_probe()

Link: https://lore.kernel.org/20260330073356.GA1017537@ax162/
Signed-off-by: Nathan Chancellor <nathan@kernel.org>
---
 drivers/net/ethernet/freescale/enetc/enetc_pf.c | 5 +++--
 1 file changed, 3 insertions(+), 2 deletions(-)

diff --git a/drivers/net/ethernet/freescale/enetc/enetc_pf.c b/drivers/net/ethernet/freescale/enetc/enetc_pf.c
index 8cb4c759b165..488c50678d80 100644
--- a/drivers/net/ethernet/freescale/enetc/enetc_pf.c
+++ b/drivers/net/ethernet/freescale/enetc/enetc_pf.c
@@ -1285,9 +1285,9 @@ static int enetc_pf_probe(struct pci_dev *pdev,
 {
 	struct device_node *node = pdev->dev.of_node;
 	struct enetc_ndev_priv *priv;
+	struct enetc_pf *pf = NULL;
 	struct net_device *ndev;
 	struct enetc_si *si;
-	struct enetc_pf *pf;
 	int err;
 
 	err = enetc_pf_register_with_ierb(pdev);
@@ -1394,7 +1394,8 @@ static int enetc_pf_probe(struct pci_dev *pdev,
 	free_netdev(ndev);
 err_alloc_netdev:
 err_setup_mac_addresses:
-	kfree(pf->vf_state);
+	if (pf)
+		kfree(pf->vf_state);
 err_alloc_vf_state:
 err_device_disabled:
 	enetc_psi_destroy(pdev);
-- 
2.53.0

